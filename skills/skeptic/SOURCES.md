# Sources — skeptic

- Job: multi-lens standalone code review (subagent per stage, evidence-labeled findings).
- Stack: **Rust-first** (form, clippy/rustfmt, ownership, type-driven APIs).
- Shape: fixed stages purpose → architecture → testability → conventions → hard-rules.
- Format: Skillet (`spec.md` + `SKILL.md` + `references/` load-on-demand).
- Shared refs under this skill (garfield-style): `write-code`, `testing`, `personal-guidelines`, `hard-rules`, `examples/`.
- Related: stage skills `skeptic-purpose` … `skeptic-hard-rules`.
